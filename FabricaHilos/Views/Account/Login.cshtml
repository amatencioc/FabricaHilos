@{
    ViewData["Title"] = "Iniciar Sesión";
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Iniciar Sesión - Fábrica de Hilos</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/lib/bootstrap-icons/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body class="login-page">
<div class="min-vh-100 d-flex align-items-center justify-content-center p-4">
    <div class="w-100" style="max-width: 420px;">
        <div class="d-flex align-items-center justify-content-center gap-3 mb-4">
            <img src="~/images/logo-colonial-icon.svg" alt="La Colonial" style="height: 56px; width: auto; flex-shrink: 0;" />
            <div>
                <h2 class="text-white fw-bold mb-0">La Colonial</h2>
                <p class="text-white-50 mb-0">Fábrica de Hilos S.A.</p>
            </div>
        </div>

        <div class="card login-card p-4">
            <h5 class="card-title text-center mb-4 fw-bold" style="color: #1a3a5c;">
                <i class="bi bi-person-circle me-2"></i>Iniciar Sesión
            </h5>

            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-danger">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <div><i class="bi bi-exclamation-triangle me-1"></i>@error.ErrorMessage</div>
                    }
                </div>
            }

            <form method="post" asp-action="Login" asp-controller="Account">
                @Html.AntiForgeryToken()
                <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />

                <div class="mb-3">
                    <label class="form-label fw-semibold">Usuario</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-person"></i></span>
                        <input type="text" name="usuario" class="form-control" placeholder="Ingrese su usuario" required autocomplete="username"
                               style="text-transform: uppercase;"
                               oninput="this.value = this.value.toUpperCase()" />
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-semibold">Contraseña</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-lock"></i></span>
                        <input type="password" name="password" class="form-control" placeholder="••••••••" required autocomplete="current-password" />
                    </div>
                </div>

                @*<div class="mb-3 form-check">
                    <input type="checkbox" name="recordarme" class="form-check-input" id="recordarme" value="true" />
                    <label class="form-check-label" for="recordarme">Recordarme</label>
                </div>*@

                <div class="d-grid">
                    <button type="submit" class="btn btn-lg fw-bold" style="background-color: #1a3a5c; color: white;">
                        <i class="bi bi-box-arrow-in-right me-2"></i>Ingresar
                    </button>
                </div>
            </form>

            @*<hr class="my-3" />
            <div class="text-center">
                <small class="text-muted">
                    <i class="bi bi-info-circle me-1"></i>
                    Credenciales por defecto: <strong>admin@fabricahilos.com</strong> / <strong>Admin123!</strong>
                </small>
            </div>*@
        </div>

        <div class="text-center mt-3">
            <small class="text-white-50">&copy; @DateTime.Now.Year La Colonial - Fábrica de Hilos S.A.</small>
        </div>
    </div>
</div>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
